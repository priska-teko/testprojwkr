# This project uses a root .gitignore for most ignores.
# Keep only frontend-specific overrides here.

# Build output stays ignored at root, but keep for clarity if used standalone
dist/
dist-ssr/

# Vite cache (root also ignores .vite/, kept for local clarity)
.vite/

